// This code is a function named 'multiply' that takes two parameters 'num1' and 'num2' and returns the product of the two numbers

// Define a function named 'multiply' that uses an arrow function to display the return value
const multiply = (num1, num2) => num1 * num2;

// Call the 'multiply' function with arguments 5 and 10, and store the result in a variable named 'result'
const result = multiply(5, 10);

// Output the value of 'result' to the console
console.log(result);
